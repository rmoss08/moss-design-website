import { Fragment } from 'react';
import Nav from './Nav';
import Menu from './Menu';

const Header = () => {
  
    return (
    <Fragment>
      <Nav />
      <Menu />
    </Fragment>
  );
};

export default Header;
